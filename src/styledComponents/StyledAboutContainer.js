// Div container that will wrap the About container 
// Purpose is to set the margin between the container and the remaining components
// such as the Work/Project container


import styled from "styled-components";

const StyledAboutContainer = styled.div`
  padding-bottom: 60px;
  background-color: rgb(24,24,24); 
`;

export default StyledAboutContainer;
