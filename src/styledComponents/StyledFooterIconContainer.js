// Style for the icon that is added in the message at the footer
// Icon will be a heart or lightning.
import styled from "styled-components";

const StyledFooterIconContainer = styled.img`
  height: 20px;
  width: 20px;
`;

export default StyledFooterIconContainer;
