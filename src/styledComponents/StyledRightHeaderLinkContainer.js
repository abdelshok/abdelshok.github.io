// Div Container placed at the top right of the navigation bar
// Holds the links to the different parts of the website: About, Projects, and Contact

import styled from "styled-components";

const StyledRightHeaderLinkContainer = styled.div`
  height: auto;
  weight: auto;
  display: inline;
  position: absolute;
  right: 5%;
  top: 50%;
`;

export default StyledRightHeaderLinkContainer;
